import React from 'react';
// import fs from 'fs';

// function getPages() {
//   const pagesDirectory = `${process.cwd()}/pages`;
//   const pages = fs.readdirSync(pagesDirectory);
//   return pages;
// }

// function Navlist() {
//   return (
//     <>
//       navlist
//       {getPages().map((filename, index) => {
//         return <li key={index}>{filename}</li>;
//       })}
//     </>
//   );
// }

function Navlist() {
  return null;
}

export default Navlist;
